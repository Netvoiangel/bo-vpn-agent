[project]
name = "bo-vpn-agent"
version = "0.1.0"
description = "BO/VPN diagnostic worker MVP"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bo-vpn-agent = "bo_vpn_agent.__main__:main"
bo-vpn-runner-daemon = "bo_vpn_agent.runner_daemon_main:main"
